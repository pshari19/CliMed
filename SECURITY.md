# Security

Do not report credentials, private licence keys or other sensitive material in public GitHub issues.

The public build must never contain:
- private signing keys;
- API tokens;
- tunnel credentials;
- passwords;
- private URLs;
- `.env` files.

Only the Ed25519 public verification key may be distributed with CliMed.
